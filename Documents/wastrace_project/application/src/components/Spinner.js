// Spinner.js sample
import React from "react";
import "../styles/Spinner.css"; // Spinner ka apna css hota hai usually

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;